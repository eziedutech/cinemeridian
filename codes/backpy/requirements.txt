# CineMeridian backend.
#
# The only AI dependencies permitted in this project are Google Cloud's.
# Nothing from any other model provider belongs in this file.

# The Agent Development Kit, installed through the Vertex AI package the
# hackathon's own setup instructions name. It pulls in google-adk; naming it
# this way keeps the dependency list matching the documented path rather than
# a package that happens to be equivalent.
google-cloud-aiplatform[adk]>=1.101.0
google-adk>=1.0.0
google-genai>=1.0.0
google-cloud-storage>=2.18.0

# The MCP *client* library, which ADK imports to speak the protocol. Pinned
# below 2.0 because google-adk 2.8 requires mcp>=1.24,<2 and MCP SDK 2.x moved
# mcp.shared.session, so an unpinned install breaks the import of McpToolset.
#
# This is not the ClickHouse MCP *server* - that is fetched by uv at runtime
# and deliberately not a dependency here.
mcp>=1.24,<2

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
sse-starlette>=2.1.0

# mcp-clickhouse is NOT installed here. It runs as a stdio subprocess launched
# by uv at runtime (see app/agent.py), which is what keeps the ClickHouse
# access on the MCP path the track requires.
